package com.nova.client.ui.theme

import androidx.compose.ui.graphics.Color

// ── Dark Theme ──────────────────────────────────────────────────────────────
object DarkColors {
    val Background    = Color(0xFF0D0D0D)
    val Surface       = Color(0xFF1A1A1A)
    val SurfaceVariant= Color(0xFF242424)
    val Primary       = Color(0xFF6C63FF)
    val PrimaryVariant= Color(0xFF8B85FF)
    val Accent        = Color(0xFF00D4FF)
    val OnBackground  = Color(0xFFE8E8E8)
    val OnSurface     = Color(0xFFCCCCCC)
    val OnPrimary     = Color(0xFFFFFFFF)
    val Error         = Color(0xFFFF4444)
    val Success       = Color(0xFF00CC66)
    val Border        = Color(0xFF2A2A2A)
}

// ── Neon Theme ───────────────────────────────────────────────────────────────
object NeonColors {
    val Background    = Color(0xFF050510)
    val Surface       = Color(0xFF0A0A20)
    val SurfaceVariant= Color(0xFF0F0F2A)
    val Primary       = Color(0xFF00FF88)
    val PrimaryVariant= Color(0xFF00CC6A)
    val Accent        = Color(0xFFFF00CC)
    val OnBackground  = Color(0xFFEEFFEE)
    val OnSurface     = Color(0xFFCCFFDD)
    val OnPrimary     = Color(0xFF000000)
    val Error         = Color(0xFFFF0055)
    val Success       = Color(0xFF00FF88)
    val Border        = Color(0xFF00FF8833)
}

// ── AMOLED Theme ─────────────────────────────────────────────────────────────
object AmoledColors {
    val Background    = Color(0xFF000000)
    val Surface       = Color(0xFF0A0A0A)
    val SurfaceVariant= Color(0xFF111111)
    val Primary       = Color(0xFFFFFFFF)
    val PrimaryVariant= Color(0xFFCCCCCC)
    val Accent        = Color(0xFF888888)
    val OnBackground  = Color(0xFFFFFFFF)
    val OnSurface     = Color(0xFFDDDDDD)
    val OnPrimary     = Color(0xFF000000)
    val Error         = Color(0xFFFF3333)
    val Success       = Color(0xFF33FF77)
    val Border        = Color(0xFF1A1A1A)
}

// ── Minimal Theme ─────────────────────────────────────────────────────────────
object MinimalColors {
    val Background    = Color(0xFFF5F5F5)
    val Surface       = Color(0xFFFFFFFF)
    val SurfaceVariant= Color(0xFFF0F0F0)
    val Primary       = Color(0xFF1A1A2E)
    val PrimaryVariant= Color(0xFF16213E)
    val Accent        = Color(0xFF0F3460)
    val OnBackground  = Color(0xFF1A1A1A)
    val OnSurface     = Color(0xFF333333)
    val OnPrimary     = Color(0xFFFFFFFF)
    val Error         = Color(0xFFCC0000)
    val Success       = Color(0xFF006633)
    val Border        = Color(0xFFE0E0E0)
}

// HUD overlay colors (always semi-transparent)
val HudTextColor      = Color(0xFFFFFFFF)
val HudBgColor        = Color(0xAA000000)
val HudAccentColor    = Color(0xFF00D4FF)
val HudWarningColor   = Color(0xFFFFAA00)
val HudDangerColor    = Color(0xFFFF4444)
